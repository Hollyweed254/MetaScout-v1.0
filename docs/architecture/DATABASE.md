# MetaScout Database Architecture

## Philosophy

The database stores facts. Never calculations. Facts are never invented — only mirrored from real eFootball data or explicitly marked as user-entered.

## Core Entity Model